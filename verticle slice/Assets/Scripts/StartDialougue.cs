using UnityEngine;

public class StartDialougue : MonoBehaviour
{
     [SerializeField] 
        private DialogueNode DialogueNode ;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
